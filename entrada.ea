start
ESCREVER "maria ",  "ana", "=", 9+2;
ESCREVER "maria", "joaquim", "=", 0;
ESCREVER "soma de ", 9, " com ", 3*4, " = ", 21;

VAR nome : "ates";
ESCREVER nome;
VAR tmp, a, b;
tmp : 7 + 3;
ESCREVER "a : 10 * (30 + tmp)";
a : 2;
b : tmp * (a + 10);
VAR c : tmp * a;
ESCREVER "A: ", a, " B: ", b, " TMP: ", tmp, " C: ", c;
end